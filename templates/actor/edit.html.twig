{% extends 'base.html.twig' %}

{% block title %}Edit Actor{% endblock %}

{% block body %}
    <h1>Edit Actor</h1>

    <a href="{{ path('app_actor_index') }}">back to list</a>
    {{ include('actor/_delete_form.html.twig') }}
    {{ include('actor/_form.html.twig', {'button_label': 'Update'}) }}
    {# } include('actor/_add_movie_form.html.twig', {'button_label': 'Add Movie'}) }#}
{% endblock %}

{% block javascripts %}

{% endblock %}